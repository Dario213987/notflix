import { TicksToTimePipe } from './ticks-to-time.pipe';

describe('TicksToTimePipe', () => {
  it('create an instance', () => {
    const pipe = new TicksToTimePipe();
    expect(pipe).toBeTruthy();
  });
});
